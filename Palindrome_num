read -p "Enter Number " n
sum=0
temp=$n
while ((n>0))
do
	rem=$((n%10))
	sum=$((sum*10+rem))
	n=$((n/10))
done
	if (($temp==$sum))
	then
		echo "$temp is a Palindrome number"
	else
		echo "$temp is not a Palindrome number"
	fi
